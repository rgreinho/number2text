============
Installation
============

At the command line::

    $ pip install number2text

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv number2text
    $ pip install number2text
